<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th scope="col">名称</th>
      <th scope="col">内容</th>
    </tr>
  </thead>

  <tbody>
    <% @show_attributes.each do |item| %>
      <tr>
        <td>
          <%= t(".#{item}") %>
        </td>
        <td>
          <%= @conference.try(item) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<!-- <%# link_to '编辑', edit_conference_path(@conference) %> | -->
<%= link_to '返回', conferences_path %>
