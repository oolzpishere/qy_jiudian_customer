<%= form_with(model: conference, local: true) do |form| %>
  <% if conference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conference.errors.count, "error") %> prohibited this conference from being saved:</h2>

      <ul>
      <% conference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% @attribute_types.each do |column_name, field_module_name| %>
    <% field = Admin::Field.new(column_name: column_name, field_module_name: field_module_name, form: form) %>
    <%= render_field(form, field) %>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= javascript_include_tag "admin/form_selectize" %>
