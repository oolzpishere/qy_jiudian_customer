<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th scope="col">名称</th>
      <th scope="col">内容</th>
    </tr>
  </thead>

  <tbody>
    <% order_data = Admin::OrderTranslate::Index.new(order: @order) %>
    <% @show_attributes.each do |item| %>
      <tr>
        <td>
          <%= t(".#{item}") %>
        </td>
        <td>
          <%= order_data.get_data(item) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<!-- <%# link_to '编辑', admin.edit_order_path(@order) %> | -->
<%= link_to '返回', request.referer || admin.admin_root_path %>
